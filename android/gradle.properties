# Disable AGP 9.0+ built-in Kotlin so org.jetbrains.kotlin.android (Kotlin 2.0.21) applies without extension conflict
android.builtInKotlin=false
android.newDsl=false

# Disable KSP2 Analysis API to prevent Room DAO "unexpected jvm signature V" bug
ksp.useKSP2=false

# AndroidX configuration
android.useAndroidX=true

# JVM arguments
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.parallel=true
